import React from "react";
import styles from "../styles/{{name}}.module.css";
import { Container, Row, Col } from "react-bootstrap";

export default function {{componentName name}}() {
  return (
    <Container className={ styles.{{cssCompliantName name}} }>
      <Row>
        <Col>
        </Col>
      </Row>
    </Container>
  );
}
